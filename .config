# base directory
PATH_CONFIG=$(realpath ${BASH_SOURCE[0]})
DIR_BASE=$(cd ${0%/*} && pwd)
SCRIPT_NAME=${0##*/}

# set dir utils
DIR_UTILS=${DIR_BASE}/utils
# source all function
source ${DIR_UTILS}/include

## module dir
MODULE_DIR=${DIR_BASE}/modules
MODULE_ENTRY_FILE="run"

## enumerate modules
MODULES=(
	"builtin_exit"
	"builtin_cd"
	"parsing"
	"perm"
	"pipe"
	"redirection"
)

PARTS=(
	"basic"
	"built"
)

PREFIX_INSTALL_MODULE=setup_
PREFIX_POST_INSTALL_MODULE=post_install_

#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#==#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#
#> CONFIG

TITLE_LENGTH=60
DEEPTHOUGHT_FILE=${DIR_BASE}/deepthought
PRINT_OFFSET_STR="___"
PRINT_OFFSET_STR="..."
PRINT_OFFSET_STR="   "
PRINT_OFFSET_STR="[+]"
PRINT_OFFSET_STR=" + "
PRINT_OFFSET_STR="   "
# default deep_lvl
DEEP_LVL=0
DFL_TAIL_ARG=2
DFL_HEAD_ARG=3
EXEC_DISABLE_DIFF=0

#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#==#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#
